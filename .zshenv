# Sourced by ALL zsh invocations, including non-interactive SSH commands
# (e.g. mosh launching mosh-server). Keep this minimal — just PATH essentials.
export PATH="/opt/homebrew/bin:/opt/homebrew/sbin:$PATH"
